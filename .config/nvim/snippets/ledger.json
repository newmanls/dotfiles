{
    "New Ledger Entry": {
        "prefix": "new",
        "body": [
            "${1:$CURRENT_YEAR}/${2:$CURRENT_MONTH}/${3:$CURRENT_DATE} ${4:Description}",
            "\t${0}"
        ],
        "description": "Create a new Ledger entry with autogenerated date and from/to accounts"
    }
}
