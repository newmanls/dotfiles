super + space
    dmenu_run_i

super + Return
    st

super + shift + q
    xdotool getwindowfocus windowkill

super + e
    st -e lfub

super + w
    firefox

super + c
    qalculate-gtk

super + shift + {equal,minus}
    notify-backlight {up,down}

super + {equal,minus,0}
    notify-pamixer {up,down,mute}

super + shift + s
    dmenu_maim

super + shift + Escape
    dmenu_session

super + alt + space
    emojipick

super + alt + m
    dmenu_mount

super + alt + u
    dmenu_unmount

super + alt + g
    dmenu_emu
